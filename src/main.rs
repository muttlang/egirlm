//Define libraries
use matrix_sdk::{
        Client, config::SyncSettings,
        ruma::{user_id, events::room::message::SyncRoomMessageEvent},
};
fn main() {
    // Staring egirlm
    println!("Starting egirlm");
    
}
